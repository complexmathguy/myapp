template: true   # required for local templates
valuesFilePath: values.yml

resources:
  - name: demoRepo
    type: GitRepo
    configuration:
      # SCM integration where the repository is located
      gitProvider: {{ .Values.demoRepo.gitProvider }} # this will be replaced from values.yml
      # Repository path, including org name/repo name
      path: {{ .Values.demoRepo.path }} # this will be replaced from values.yml
      branches:
        # Specifies which branches will trigger dependent steps
        include: master

pipelines:
  - name: demoPipeline
    configuration:
      runtime:
        type: image
        image:
          auto:
            language: java
            versions:
            - "8"
    steps:
      - name: build_step
        type: Bash
        configuration:
          inputResources:
            - name: demoRepo     # Trigger execution on code commit
        execution:
          onExecute:
            - sudo apt-get install -y maven
            - mvn test
            - mkdir -p JFrog_Pipelines/testresults
            - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} JFrog_Pipelines/testresults/ \;
          onSuccess:
            - echo "Build step using Maven is complete!"     
            
      - name: docker_build_and_push_step
        type: Bash
        configuration:
          runtime:      
            type: image
            image:
              custom:
                name: docker:docker
                tag: latest
                options: "-e HOME=/root" 
        execution:
          onExecute:
          - docker login --username complexmathguy --password 00Corvette
          - docker build -t realmethods/myapp:latest .
          - docker push realmethods/myapp:latest
          onSuccess:
            - echo "Docker build and push step is complete!"            
